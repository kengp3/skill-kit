# project-setting 最終方案整理計畫

狀態：2026-09-16 已完成技能與文件精簡；平台驗證仍待進行。任務清單見 [todo.md](todo.md)。

## 目標與範圍

依使用者確認，本功能仍處於規劃階段，不需要支援歷次設計的遷移。交付文件只呈現最終方案與必要操作，不保留舊 Python 路由、AGENTS 區塊、舊設定轉換或歷史相容流程。

本計畫取代同一工作的先前計畫。整理僅限 project-setting 技能、README 與其專屬設計文件；保留其他技能、使用者內容與無關未提交變更。本次僅精簡技能與文件，未安裝至真實專案、提交或推送。

## 最終設計

- 專案根目錄的 project-setting.md 是文件分類、命名與位置的唯一規範來源。根目錄由使用者或工作階段確認，不依賴 Git 或 AGENTS.md 定位。
- 技能涵蓋初始化、日常使用、更新與停用。已有規範須保留自訂內容，更新只改授權項目。
- SessionStart／SubagentStart 的 Hook（事件掛鉤）只輸出固定提醒。保留現有兩平台範本與命令，不新增事件、腳本或防竄改機制。
- 技能使用者不需要 Python／Git；維護者測試與安裝工具的需求分開說明。
- 保留五種預設分類、疑義詢問、規範自身排除、路徑與符號連結邊界、防覆寫及實際路徑核對。平台適用指令仍須遵循。
- 停用只移除可確認屬於本技能的提醒，保留其他 Hook；預設保留規範與文件。自訂過的提醒須確認歸屬，不能整組刪除。

## 實作順序

1. 精簡技能：移除遷移指南與所有流程引用，將必要停用步驟併入既有 configuration.md。
2. 同步交付文件：README 與規格只描述最終功能；清除被取代的設計文件與其引用。
3. 檢查資源連結、保留的行為規範、命令測試及文件一致性，記錄結果。

詳細檔案、依賴與驗收見任務清單。工作小且相依，循序處理即可。

## 驗證與完成條件

- 技能、README 與有效規格不再提供遷移入口、舊格式轉換或歷史路由指引。所有內部連結仍有效。
- 初始化、更新、停用均可僅靠技能包內資源理解並執行；保留自訂規範、其他 Hook 與既有文件。
- 執行 `python3 -B -m unittest discover -s tests -v` 與 `git diff --check`。這是維護者驗證，不是使用者的執行依賴；文字整理不新增只比對措辭的測試。
- 對照資源前後差異，確認兩平台命令未變、規範只刪除歷史說明，安全與分類行為未被移除。
- 本次整理完成不代表平台驗收完成。原生 Windows、平台信任／事件載入、模型建立／修改文件與缺失／衝突處理，仍須實際環境驗證才能標示通過。

## 驗證紀錄

- 維護者測試：`python3 -B -m unittest discover -s tests -v`，2 通過、1 因需要原生 Windows cmd.exe 跳過。
- 實作前後比對：兩平台 Hook 範本、現有測試與其他技能內容保持不變；規範範本只刪除歷史來源說明。
- 內部連結目標存在性、刪除文件引用搜尋及 `git diff --check` 通過；本次變動僅限計畫列出的 11 個檔案（包含刪除）。
- 文字走讀：全新初始化僅在規範不存在時建立；重複初始化保留自訂規範且不重複提醒；更新只改授權項目；混合 Hook 停用保留其他 handler，自訂提醒歸屬不明先釐清。
- 已刪除遷移指南及兩份重複舊計畫；交付文件只保留最終操作與有效來源，停用併入設定說明。
- 原生 Windows、Codex／Claude 信任及事件載入、模型初始化與文件操作尚未實測。上述走讀與命令測試不代表模型驗收。
- 未新增測試或執行依賴，未安裝、初始化真實專案、提交或推送。
